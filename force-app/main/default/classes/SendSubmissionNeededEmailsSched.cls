global class SendSubmissionNeededEmailsSched  implements Schedulable {
    
    
    /*** SCHEDULABLE INTERFACE Method ***/
    global void execute(SchedulableContext sc) {
        //id batchId = Database.executeBatch(new SendSubmissionNeededEmails());
        //System.debug('\n\n==> batchId = ' + batchId);
    }
    
    
}